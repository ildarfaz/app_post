import { Pagination } from "../pagination/Pagination"
import { Table } from "../ui/table/Table"

export const Content = () => {
    return (
        <div>
            <Table />
            <Pagination />
        </div>
    )
}